//import the handlers from the auth file
import {handlers} from '@/src/auth'

export const {GET,POST}=handlers    //This means that this route will take care of our sign in, sign out and callback handlers
